<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      enableOnBackInvokedCallback is not optional here. Without it the framework logs
      "OnBackInvokedCallback is not enabled for the application" and every BackHandler in
      the app silently receives nothing — back appears to do nothing at all, on every
      screen. targetSdk 35 does not turn it on by itself.
    -->
    <application
        android:name=".BookiesApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bookies">

        <!--
          Asks Play services to fetch the barcode-scanning module when Bookies is
          installed rather than on the first scan. Without it the first attempt to scan a
          book fails with CODE_SCANNER_UNAVAILABLE while the module downloads, which looks
          exactly like a broken feature. Nothing here requests the CAMERA permission: the
          scan runs inside Play services' own activity, not in this app.
        -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Bookies"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- "Open with Bookies" from a browser download or file manager. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/epub+zip" />
                <data android:scheme="file" android:mimeType="application/epub+zip" />
            </intent-filter>

            <!-- Share sheet target. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/epub+zip" />
            </intent-filter>
        </activity>

        <!--
          Everything Bookies writes is app-private, so a Markdown export cannot be handed
          to another app as a file:// path at all. This is the seam that makes the share
          sheet possible, scoped in res/xml/file_paths.xml to the export directory alone.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
